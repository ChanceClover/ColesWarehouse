# Core pipeline scripts use only the Python standard library.
# Pillow is needed only for powerbi/build_final_evidence.py, which creates image-based dashboard evidence.
Pillow>=10.0.0
streamlit>=1.24.0
pandas>=2.0.0
